const path = require("path");

const ROOT_PATH = "compiled_all/server/";

module.exports = {
    "config": path.resolve(ROOT_PATH, "utils/sequelize_cli_config.js"),
    "migrations-path": path.resolve(ROOT_PATH, "migrations"),
    // "models-path": path.resolve(ROOT_PATH + "models", "models"),
}
